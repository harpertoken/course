# SPDX-License-Identifier: MIT

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "systemmanager-anomalib"
version = "0.1.0"
description = "Anomalib integration for SystemManager"
dependencies = [
    "anomalib>=2.0.0"
]